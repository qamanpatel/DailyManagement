<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:DailyManagementSystem.ViewModels"
             mc:Ignorable="d" d:DesignWidth="1100" d:DesignHeight="800"
             x:Class="DailyManagementSystem.Views.ReportView"
             x:DataType="vm:ReportViewModel">

    <Grid RowDefinitions="Auto,Auto,*" Margin="20">
        
        <!-- Header & Filters -->
        <Grid Grid.Row="0" ColumnDefinitions="Auto,*,Auto" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal" Spacing="15">
                <Button Command="{Binding BackCommand}" Content="← Back" />
                <TextBlock Text="Financial Report" FontSize="24" FontWeight="Bold" VerticalAlignment="Center" Foreground="White"/>
                <Border Background="#37474F" Padding="10,5" CornerRadius="5" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock Text="Role:" Foreground="#B0BEC5" FontSize="11" VerticalAlignment="Center"/>
                        <TextBlock Text="Admin" IsVisible="{Binding IsAdmin}" FontWeight="SemiBold" Foreground="#90CAF9" FontSize="11" VerticalAlignment="Center"/>
                        <TextBlock Text="User" IsVisible="{Binding !IsAdmin}" FontWeight="SemiBold" Foreground="#81C784" FontSize="11" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="15">
                <!-- From Section -->
                <StackPanel Orientation="Horizontal" Spacing="5">
                    <TextBlock Text="From:" VerticalAlignment="Center" Foreground="#B0BEC5"/>
                    <ComboBox ItemsSource="{Binding Months}" SelectedItem="{Binding FromMonth}" Width="80" PlaceholderText="All"/>
                    <ComboBox ItemsSource="{Binding Years}" SelectedItem="{Binding FromYear}" Width="100" PlaceholderText="All"/>
                </StackPanel>
                
                <!-- To Section -->
                <StackPanel Orientation="Horizontal" Spacing="5">
                    <TextBlock Text="To:" VerticalAlignment="Center" Foreground="#B0BEC5"/>
                    <ComboBox ItemsSource="{Binding Months}" SelectedItem="{Binding ToMonth}" Width="80" PlaceholderText="All"/>
                    <ComboBox ItemsSource="{Binding Years}" SelectedItem="{Binding ToYear}" Width="100" PlaceholderText="All"/>
                </StackPanel>

                <Button Command="{Binding LoadReportCommand}" Content="Apply Filter" Classes="accent"/>
                <Button Command="{Binding ClearFilterCommand}" Content="Clear"/>
                
                <Button Command="{Binding DownloadReportCommand}" 
                        Content="Download Report" 
                        IsEnabled="{Binding !IsDownloading}"/>
            </StackPanel>
        </Grid>

        <!-- Validation / Error Message -->
        <TextBlock Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Bottom"
                   Text="{Binding ErrorMessage}" Foreground="Red" FontSize="12" Margin="0,0,0,5"
                   IsVisible="{Binding ErrorMessage, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"/>

        <!-- Summary Metrics Cards -->
        <UniformGrid Grid.Row="1" Columns="5" Margin="0,0,0,20">
            <Border Background="#1E1E1E" BorderBrush="#333333" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="Total Orders" Foreground="#90CAF9" FontSize="12"/>
                    <TextBlock Text="{Binding TotalOrders}" FontSize="20" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="{Binding TotalOrderAmount, StringFormat={}{0:C}}" FontSize="11" Foreground="#90CAF9"/>
                </StackPanel>
            </Border>
            
            <Border Background="#1E1E1E" BorderBrush="#333333" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="Income" Foreground="#A5D6A7" FontSize="12"/>
                    <TextBlock Text="{Binding TotalIncome, StringFormat={}{0:C}}" FontSize="20" FontWeight="Bold" Foreground="White"/>
                </StackPanel>
            </Border>

            <Border Background="#1E1E1E" BorderBrush="#333333" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="Expense" Foreground="#EF9A9A" FontSize="12"/>
                    <TextBlock Text="{Binding TotalSpent, StringFormat={}{0:C}}" FontSize="20" FontWeight="Bold" Foreground="White"/>
                </StackPanel>
            </Border>

            <Border Background="#1E1E1E" BorderBrush="#333333" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="Pending" Foreground="#FFB74D" FontSize="12"/>
                    <TextBlock Text="{Binding PendingAmount, StringFormat={}{0:C}}" FontSize="20" FontWeight="Bold" Foreground="White"/>
                </StackPanel>
            </Border>

            <Border Background="#1E1E1E" BorderBrush="#333333" BorderThickness="1" CornerRadius="8" Padding="15">
                <StackPanel>
                    <TextBlock Text="Net Profit" Foreground="#CE93D8" FontSize="12"/>
                    <TextBlock Text="{Binding NetProfit, StringFormat={}{0:C}}" FontSize="20" FontWeight="Bold" Foreground="White"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- Detailed Breakdown Tabs -->
        <TabControl Grid.Row="2" CornerRadius="8" Background="#1E1E1E" BorderBrush="#333333" BorderThickness="1">
            
            <!-- Orders Tab -->
            <TabItem Header="Orders">
                <DataGrid ItemsSource="{Binding OrderReports}" IsReadOnly="True" AutoGenerateColumns="False" CanUserResizeColumns="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat='{}{0:dd MMM yyyy}'}" Width="150"/>
                        <DataGridTextColumn Header="Client" Binding="{Binding ClientName}" Width="*"/>
                        <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat={}{0:C}}" Width="150"/>
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <!-- Payments Tab -->
            <TabItem Header="Receipts">
                <DataGrid ItemsSource="{Binding PaymentReports}" IsReadOnly="True" AutoGenerateColumns="False" CanUserResizeColumns="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat='{}{0:dd MMM yyyy}'}" Width="150"/>
                        <DataGridTextColumn Header="Client" Binding="{Binding ClientName}" Width="*"/>
                        <DataGridTextColumn Header="Bank" Binding="{Binding BankName}" Width="120"/>
                        <DataGridTextColumn Header="Order/Ref" Binding="{Binding OrderName}" Width="1.5*"/>
                        <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat={}{0:C}}" Width="150"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <!-- Expenses Tab -->
            <TabItem Header="Expenses">
                <DataGrid ItemsSource="{Binding ExpenseReports}" IsReadOnly="True" AutoGenerateColumns="False" CanUserResizeColumns="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat='{}{0:dd MMM yyyy}'}" Width="150"/>
                        <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="150"/>
                        <DataGridTextColumn Header="Spent By" Binding="{Binding SpentBy}" Width="150"/>
                        <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                        <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat={}{0:C}}" Width="150"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <!-- Category Summary Tab -->
            <TabItem Header="Category Summary">
                <DataGrid ItemsSource="{Binding CategoryExpenseReports}" IsReadOnly="True" AutoGenerateColumns="False" CanUserResizeColumns="True" HorizontalAlignment="Left" Width="400">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="*"/>
                        <DataGridTextColumn Header="Total Spent" Binding="{Binding TotalAmount, StringFormat={}{0:C}}" Width="150"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <!-- Person Summary Tab -->
            <TabItem Header="Spent By Summary">
                <DataGrid ItemsSource="{Binding PersonExpenseReports}" IsReadOnly="True" AutoGenerateColumns="False" CanUserResizeColumns="True" HorizontalAlignment="Left" Width="400">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Person" Binding="{Binding SpentBy}" Width="*"/>
                        <DataGridTextColumn Header="Total Spent" Binding="{Binding TotalAmount, StringFormat={}{0:C}}" Width="150"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

        </TabControl>

    </Grid>
</UserControl>
