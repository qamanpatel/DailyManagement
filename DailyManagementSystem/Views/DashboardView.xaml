<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:DailyManagementSystem.ViewModels"
             mc:Ignorable="d" d:DesignWidth="1000" d:DesignHeight="700"
             x:Class="DailyManagementSystem.Views.DashboardView"
             x:DataType="vm:DashboardViewModel">
    
    <Grid RowDefinitions="Auto,Auto,*" Margin="20">
        
        <!-- Header & Filters -->
        <Grid Grid.Row="0" ColumnDefinitions="Auto,*,Auto,Auto" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal" Spacing="15">
                <TextBlock Text="Dashboard" VerticalAlignment="Center" FontSize="28" FontWeight="Bold" Foreground="White"/>
                <Border Background="#37474F" Padding="10,5" CornerRadius="5" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock Text="User:" Foreground="#B0BEC5" FontSize="12" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding CurrentUserName}" FontWeight="SemiBold" Foreground="#90CAF9" FontSize="12" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </StackPanel>
            
            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="10">
                <ComboBox ItemsSource="{Binding Months}" SelectedItem="{Binding SelectedMonth}" Width="100" PlaceholderText="All"/>
                <ComboBox ItemsSource="{Binding Years}" SelectedItem="{Binding SelectedYear}" Width="100" PlaceholderText="All"/>
                <Button Command="{Binding ApplyFilterCommand}" Content="Apply Filter" Classes="accent"/>
                <Button Command="{Binding RefreshCommand}" Content="Refresh"/>
                <Button Command="{Binding LogoutCommand}" Content="Logout" Foreground="#EF5350"/>
            </StackPanel>
        </Grid>

        <!-- Metrics Cards -->
        <WrapPanel Grid.Row="1" ItemWidth="220" ItemHeight="120">
            <!-- Total Orders -->
             <Border CornerRadius="8" Background="#1E1E1E" BorderBrush="#333333" BorderThickness="1" Margin="0,0,15,15" Padding="15">
                <StackPanel>
                    <TextBlock Text="Total Orders" Foreground="#90CAF9" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding TotalOrders}" FontSize="28" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

            <!-- Total Order Amount -->
            <Border IsVisible="{Binding IsAdmin}" CornerRadius="8" Background="#1E1E1E" BorderBrush="#333333" BorderThickness="1" Margin="0,0,15,15" Padding="15">
                <StackPanel>
                    <TextBlock Text="Total Order Amount" Foreground="#FFCC80" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding TotalOrderAmount, StringFormat={}{0:C}}" FontSize="28" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

            <!-- Received -->
            <Border IsVisible="{Binding IsAdmin}" CornerRadius="8" Background="#1E1E1E" BorderBrush="#333333" BorderThickness="1" Margin="0,0,15,15" Padding="15">
                <StackPanel>
                    <TextBlock Text="Received" Foreground="#A5D6A7" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding TotalReceivedAmount, StringFormat={}{0:C}}" FontSize="28" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

            <!-- Pending -->
            <Border IsVisible="{Binding IsAdmin}" CornerRadius="8" Background="#1E1E1E" BorderBrush="#333333" BorderThickness="1" Margin="0,0,15,15" Padding="15">
                <StackPanel>
                    <TextBlock Text="Pending (This Month)" Foreground="#EF9A9A" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding TotalPendingAmount, StringFormat={}{0:C}}" FontSize="28" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

             <!-- Expenses -->
            <Border IsVisible="{Binding IsAdmin}" CornerRadius="8" Background="#1E1E1E" BorderBrush="#333333" BorderThickness="1" Margin="0,0,15,15" Padding="15">
                <StackPanel>
                    <TextBlock Text="Expenses" Foreground="#CE93D8" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding TotalExpenses, StringFormat={}{0:C}}" FontSize="28" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

             <!-- Profit -->
            <Border IsVisible="{Binding IsAdmin}" CornerRadius="8" Background="#1E1E1E" BorderBrush="#333333" BorderThickness="1" Margin="0,0,15,15" Padding="15">
                <StackPanel>
                    <TextBlock Text="Profit (Cash)" Foreground="#80DEEA" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding Profit, StringFormat={}{0:C}}" FontSize="28" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>
        </WrapPanel>

        <!-- Quick Actions -->
        <Border Grid.Row="2" Margin="0,20,0,0" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0" Padding="0,20,0,0">
            <StackPanel Spacing="10">
                <TextBlock Text="Quick Actions" FontWeight="SemiBold" FontSize="18" Margin="0,0,0,10"/>
                <WrapPanel>
                    <Button Command="{Binding AddClientCommand}" Content="Add Client" Margin="0,0,10,10" Padding="15,10"/>
                    <Button Command="{Binding AddOrderCommand}" Content="Add Order" Margin="0,0,10,10" Padding="15,10"/>
                    <Button Command="{Binding AddPaymentCommand}" Content="Add Payment" Margin="0,0,10,10" Padding="15,10"/>
                    <Button Command="{Binding AddExpenseCommand}" Content="Add Expense" Margin="0,0,10,10" Padding="15,10"/>
                    <Button Command="{Binding GenerateReportCommand}" 
                            Content="Generate Report" 
                            Classes="accent" Margin="0,0,10,10" Padding="15,10"/>
                </WrapPanel>
            </StackPanel>
        </Border>

    </Grid>
</UserControl>
